server:
  port: 8080
  servlet:
    context-path: /api/v1/hz/client

management:
  server:
    port: 8081
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: ALWAYS

spring:
  application:
    name: Hazecast-Client